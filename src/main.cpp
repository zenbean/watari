#include <iostream>
#include <vector>
#define EMPTY 0
#define WHITE 1
#define BLACK -1

// 2d coords to 1d position
int indx(int x, int y){
    int position = y*size+x;
    return position;
}

int count_neighbours?

int main(){
    int size = 9;
    std::vector<int> board(size*size, 0);
}